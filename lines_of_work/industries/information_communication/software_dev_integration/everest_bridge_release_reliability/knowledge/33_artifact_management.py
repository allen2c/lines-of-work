"""Artifact management for releases."""

title = "Artifact Management"

content = """
Release artifacts (binaries, images, configs) must be versioned, stored, and traceable. Use a
central registry or artifact repository. Tag artifacts with release identifier and build metadata.
Ensure immutability: once published, artifacts are not overwritten. Support retrieval of any
historical artifact for rollback. Document retention policy. Sign artifacts where compliance
requires provenance verification.
"""

version = "0.0.1"
