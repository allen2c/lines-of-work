title = "ETL 기초"

content = """
ETL(Extract, Transform, Load)은 소스에서 데이터를 추출하고, 변환한 뒤,
대상 시스템에 적재하는 프로세스입니다. 배치 통합의 핵심 패턴입니다.

**Extract:** DB 쿼리, API 호출, 파일 읽기로 데이터를 가져옵니다. 증분 추출
(타임스탮프, 시퀀스)로 부하를 줄입니다. 대용량은 스트리밍 또는 청크 단위로
처리합니다.

**Transform:** 정제, 매핑, 집계, 검증을 수행합니다. 데이터 품질 규칙을 적용하고
이상치는 로그 또는 별도 테이블로 분리합니다. 멱등성이 되도록 설계합니다.

**Load:** 전체 재적재 또는 upsert로 대상에 씁니다. 트랜잭션 경계를 명확히 하고
실패 시 롤백 가능하게 합니다. 적재 후 검증 단계로 레코드 수, 체크섬을 확인합니다.
"""  # noqa: E501

version = "0.0.1"
