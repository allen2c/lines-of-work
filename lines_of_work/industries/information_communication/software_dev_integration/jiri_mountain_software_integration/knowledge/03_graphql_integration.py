title = "GraphQL 통합"

content = """
GraphQL은 클라이언트가 필요한 데이터만 요청할 수 있는 쿼리 언어 및 런타임입니다.
REST에 비해 오버페칭·언더페칭 문제를 줄이고, 단일 엔드포인트로 여러 리소스를
조회할 수 있습니다.

**핵심 개념:** 스키마로 타입과 필드를 정의합니다. 쿼리(조회), 뮤테이션(변경),
구독(실시간) 연산을 지원합니다. 클라이언트는 요청 시 필요한 필드만 명시합니다.

**통합 시 적용:** N+1 쿼리 문제를 피하기 위해 DataLoader 등 배치 로딩을 고려합니다.
rate limiting은 쿼리 복잡도 기반으로 적용할 수 있습니다. Introspection을 프로덕션에서
비활성화할지 결정합니다. 에러 응답 구조를 표준화하여 클라이언트가 일관되게 처리하도록 합니다.
"""  # noqa: E501

version = "0.0.1"
