title = "데이터 매핑과 변환"

content = """
통합 시 각 시스템의 데이터 모델이 다르므로 매핑과 변환 레이어가 필요합니다.
일관된 규칙으로 변환하면 오류를 줄이고 유지보수를 쉽게 합니다.

**매핑:** 필드 대응표를 정의하고 null, 기본값, 필수/선택 처리를 명시합니다.
타입 변환(문자열↔숫자, 날짜 포맷, 인코딩) 규칙을 표준화합니다.

**변환:** 집계, 분할, 정규화, 비정규화를 변환 규칙으로 정의합니다. 비즈니스 규칙
(계산식, 검증, 기본값 부여)을 별도 모듈로 분리해 테스트 가능하게 합니다.

**도구:** 수동 매핑 코드 외에, JSONata, Jolt, 템플릿 엔진 등으로 선언적 변환을
구현할 수 있습니다. 스키마 변경 시 매핑 문서와 함께 버전을 관리합니다.
"""  # noqa: E501

version = "0.0.1"
