title = "멱등성(Idempotency)"

content = """
멱등성은 동일 요청을 여러 번 수행해도 결과가 한 번 수행한 것과 동일함을
의미합니다. 재시도·네트워크 장애 상황에서 중복 처리를 방지합니다.

**POST 요청:** 생성 작업은 기본적으로 멱등하지 않습니다. Idempotency-Key 헤더에
클라이언트가 생성한 고유 키를 넣어, 같은 키로 재요청 시 기존 결과를 반환하게
할 수 있습니다. 서버는 키-결과 매핑을 짧은 시간 캐시합니다.

**구현:** DB에 unique 제약으로 중복 키를 차단할 수 있습니다. 이미 처리된
키에 대해서는 200과 저장된 응답을 반환합니다. 처리 중인 키는 409 Conflict로
클라이언트에 재시도 지연을 요청할 수 있습니다.
"""  # noqa: E501

version = "0.0.1"
