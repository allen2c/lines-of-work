title = "서킷 브레이커"

content = """
서킷 브레이커는 연속 실패 시 추가 호출을 차단해 리소스 낭비와 연쇄 장애를
방지합니다. 마이크로서비스와 외부 API 통합에서 중요한 패턴입니다.

**상태:** Closed(정상 호출), Open(호출 차단), Half-Open(제한적 재시도) 세 가지가
있습니다. 실패 임계값을 넘으면 Open으로 전환하고, 일정 시간 후 Half-Open으로
한 건 시도합니다. 성공하면 Closed로 돌아갑니다.

**구현:** Hystrix, resilience4j, Polly 등 라이브러리를 사용할 수 있습니다.
임계값(실패 횟수, 비율)과 대기 시간을 튜닝합니다. Open 상태에서는 즉시 실패
또는 fallback 응답을 반환해 호출자가 대기하지 않게 합니다.
"""  # noqa: E501

version = "0.0.1"
