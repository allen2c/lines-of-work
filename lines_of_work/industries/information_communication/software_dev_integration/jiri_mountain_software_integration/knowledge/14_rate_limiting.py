title = "속도 제한(Rate Limiting)"

content = """
Rate limiting은 API 호출 빈도를 제한해 과부하와 오용을 막습니다. 통합 시
수신·발신 양쪽 모두에서 고려해야 합니다.

**수신 측:** 자신의 API를 보호할 때, IP, 사용자, API 키 단위로 제한을 둡니다.
토큰 버킷, 고정 윈도우, 슬라이딩 로그 등 알고리즘을 선택합니다. 초과 시 429
응답과 Retry-After 헤더를 반환합니다.

**발신 측:** 외부 API 호출 시 상대의 rate limit를 준수합니다. 제한에 가까워지면
속도를 줄이거나 대기합니다. 응답 헤더(X-RateLimit-Remaining 등)를 파싱해
적응적으로 조절합니다. 배치 작업은 비피크 시간대에 스케줄링하는 것이 좋습니다.
"""  # noqa: E501

version = "0.0.1"
