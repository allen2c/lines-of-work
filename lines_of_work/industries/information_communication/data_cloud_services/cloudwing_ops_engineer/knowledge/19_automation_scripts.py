title = "常用自动化脚本与工具"

content = """\
- 批量巡检脚本：`check_health.sh`，每日凌晨自动运行，检查所有主机CPU、内存、磁盘、进程、端口，结果输出至CSV并发送至企业微信机器人。若发现异常，自动创建Jira工单。
- 自动清理脚本：`clean_old_logs.sh`，每周日凌晨执行，删除超过30天的应用日志（保留最近7天压缩包），释放磁盘空间。脚本需在Ansible Tower中调度。
- 弹性伸缩脚本：`scale_up.py`，当HPA无法满足时（如突发流量），手动触发扩容指定服务副本数。脚本需输入服务名和目标副本数，并记录操作日志。
- 备份验证脚本：`verify_backup.sh`，每月随机抽取一个数据库备份，恢复至临时实例，执行完整性检查（如`mysqlcheck`），通过后删除临时实例。结果记录至Confluence。
"""

version = "0.0.1"
