title = "Metadata Management"

content = """
Metadata includes schema definitions, descriptions, ownership, tags, and usage
statistics. Centralized metadata enables discoverability, governance, and
automation. Rhein Cloud maintains metadata in a data catalog integrated with
Snowflake, Databricks, and Kafka. Key practices: document every table and column
with business descriptions; assign clear owners; use consistent tagging for
classification (PII, confidential, public); track last refresh and row counts.
Metadata should be updated as part of pipeline deployment, not as an afterthought.
"""

version = "0.0.1"
