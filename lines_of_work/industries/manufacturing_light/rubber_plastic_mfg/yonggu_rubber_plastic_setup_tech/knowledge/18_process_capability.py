title = "製程能力監控（Cp/Cpk）"

content = """
- 定義：Cp（製程潛在能力）與Cpk（製程實際能力）用於評估製程穩定度。Cp = (USL - LSL) / (6σ)，Cpk = min[(USL - μ)/(3σ), (μ - LSL)/(3σ)]。
- 目標值：對於關鍵尺寸（如裝配孔徑），Cpk需大於1.33；對於一般尺寸，Cpk需大於1.0。若Cpk小於1.0，表示製程變異過大，需進行調整。
- 監控方式：每小時抽樣5件成品，量測關鍵尺寸，並繪製X-bar與R管制圖。若數據超出管制界限（±3σ），需立即檢查參數是否偏移。
- 改善行動：若Cpk不足，可先檢查原料批次是否穩定，再調整參數（如降低射出壓力變異、穩定模溫）。若仍無法改善，需與工程師討論修改模具或規格。
"""

version = "0.0.1"
