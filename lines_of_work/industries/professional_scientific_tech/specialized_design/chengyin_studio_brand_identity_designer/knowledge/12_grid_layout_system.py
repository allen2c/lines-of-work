title = "版面網格系統"

content = """
版面網格（grid）讓各種應用物件保持一致的節奏與對齊，是規範書的隱形骨架。
設定：
- 欄位網格：印刷品常用 12 欄或 8 欄，方便彈性分割；標注欄寬、欄間距（gutter）與外邊界（margin）。
- 基線網格：以內文行高為基準，讓文字跨欄仍對齊。
- 安全邊界：印刷品四周留白建議至少 5 mm，重要內容不壓到裁切線。
應用：名片、DM、簡報、社群圖與網頁各自建立對應網格，但沿用同一套間距邏輯（如 4 或 8 的倍數）。
對齊原則：同一版面內元素靠網格對齊，避免隨意擺放；留白視為設計元素而非浪費。
規範書提供 2–3 個套用範例（如封面、內頁、社群貼文），示範網格如何實際運用，降低後續執行者的落差。
"""

version = "0.0.1"
