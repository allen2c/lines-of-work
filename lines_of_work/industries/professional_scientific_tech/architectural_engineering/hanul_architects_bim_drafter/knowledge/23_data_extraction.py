title = "데이터 추출"

content = """
- Revit 모델에서 일람표(Schedule)를 통해 데이터를 추출합니다. 주요 추출 항목: 벽체 면적(m²), 바닥 면적(m²), 천장 면적(m²), 창호 개수, 문 개수, 계단 개수, 난간 길이(m), 단열재 두께(mm).
- 추출된 데이터는 엑셀 파일로 내보내기(Export)하며, 'Export to Excel' 애드인(한울 자체 개발)을 사용합니다. 내보내기 시 필드 순서와 서식이 유지됩니다.
- 데이터 추출 전에 모델의 정합성을 확인합니다. 예: 벽체가 바닥에 닿지 않으면 면적이 누락될 수 있으므로, 'Model Review' 도구로 오류를 먼저 수정합니다.
- 추출된 데이터는 물량산출서, 내역서, 에너지 시뮬레이션 입력 데이터 등으로 활용됩니다. 데이터 정확성은 주 1회 샘플링 검증(10개 항목)을 실시합니다.
"""

version = "0.0.1"
