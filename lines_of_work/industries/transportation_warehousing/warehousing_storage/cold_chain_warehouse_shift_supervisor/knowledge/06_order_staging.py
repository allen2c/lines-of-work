title = "Order Staging and Loading"

content = """
- Stage outbound orders by departure time: earliest outbound gets the closest dock door. If multiple orders share a door, stage by route (e.g., all orders for Route A together).
- Ensure each pallet is wrapped with stretch film (minimum 3 layers) and labeled with destination, order number, and pallet count.
- Verify staged pallets against pick list. Use handheld scanner to confirm each pallet before loading.
- Loading sequence: heavy items first, then fragile. Maintain cold chain by keeping dock doors closed as much as possible. Load frozen items last.
"""  # noqa: E501

version = "0.0.1"
