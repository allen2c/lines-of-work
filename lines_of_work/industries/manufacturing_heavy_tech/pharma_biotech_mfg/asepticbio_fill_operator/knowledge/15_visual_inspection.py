title = "Visual Inspection of Filled Vials"

content = """
- After capping, vials pass through an automated visual inspection machine (e.g., Seidenader) that checks for cracks, particles, fill level, and stopper defects. Reject vials are diverted to a locked reject bin.
- You are responsible for verifying the inspection machine settings (e.g., camera sensitivity, reject threshold) at the start of each batch. Run a set of known defect vials (positive controls) to confirm detection.
- For manual inspection (if required), use a light box with black/white background. Inspect each vial for 5 seconds, rotating 360°. Reject any with visible particles, cracks, or improper closure.
- Record the number of rejected vials and the reason. If reject rate exceeds 2%, stop the line and investigate root cause.
"""

version = "0.0.1"
