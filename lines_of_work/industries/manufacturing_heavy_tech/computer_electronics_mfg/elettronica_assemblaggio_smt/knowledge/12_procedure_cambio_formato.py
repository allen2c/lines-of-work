title = "Procedure di cambio formato (SMED)"

content = """
- Il cambio formato (setup) include sostituzione feeder, stencil, programma macchina e verifica primo pezzo.
- Tempo target per cambio formato standard: < 15 minuti (SMED). Per cambi complessi (es. cambio linea completa): < 45 minuti.
- Fasi: preparazione esterna (preparare feeder e nastri prima del fermo), cambio interno (fermo macchina), verifica primo pezzo (AOI + test).
- Documentare ogni cambio con checklist e registrare i tempi nel MES per analisi di miglioramento continuo.
"""

version = "0.0.1"
